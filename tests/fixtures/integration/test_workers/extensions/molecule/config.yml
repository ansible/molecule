---
prerun: false
ansible:
  executor:
    args:
      ansible_playbook:
        - --inventory=${MOLECULE_SCENARIO_DIRECTORY}/../inventory.yml
scenario:
  test_sequence:
    - prepare
    - converge
    - verify
shared_state: true
slice: 1
