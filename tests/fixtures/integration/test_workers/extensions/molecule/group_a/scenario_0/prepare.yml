---
- name: prepare for group_a/scenario_0
  hosts: all
  gather_facts: false
  tasks:
    - name: "prepare debug"
      ansible.builtin.debug:
        msg: "STEP_PREPARE_SCENARIO_group_a/scenario_0"
