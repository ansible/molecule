---
- name: Converge
  hosts: molecule
  gather_facts: true
  tasks:
    - name: Create test file
      ansible.builtin.copy:
        content: "Hello from converge"
        dest: /tmp/converge.txt
        force: false
        mode: "0644"
