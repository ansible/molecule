---
- name: Converge
  hosts: all
  collections:
    - community.general
  gather_facts: false
  tasks:
    - name: Touch a file
      command: touch /tmp/cleanup
      args:
        creates: /tmp/cleanup
