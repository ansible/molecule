---
dependency:
  name: gilt
driver:
  name: delegated
platforms:
  - name: instance
    image: ${TEST_BASE_IMAGE}
provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: ../../../../../resources/roles/
scenario:
  name: gilt
verifier:
  name: ansible
