---
dependency:
  name: galaxy
driver:
  name: delegated
platforms:
  - name: instance
    image: ${TEST_BASE_IMAGE}
provisioner:
  name: ansible
scenario:
  name: ansible-galaxy
verifier:
  name: ansible
