---
dependency:
    name: $DEPENDENCY_NAME
driver:
    name: docker
lint:
    name: ansible-lint
platforms:
  - name: instance-1
provisioner:
    name: ansible
scenario:
    name: default
verifier:
    name: ${VERIFIER_NAME}
