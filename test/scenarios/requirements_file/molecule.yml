---
dependencies:
  requirements_file: requirements.yml
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: requirements-file-scenario
      image: ubuntu
      image_version: latest
verifier:
  name: testinfra
