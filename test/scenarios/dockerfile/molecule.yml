---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: dockerfile-scenario
      image: alpine
      image_version: latest
      dockerfile: Dockerfile
      command: /bin/bash
verifier:
  name: testinfra
